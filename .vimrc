" dynamic line numbers
set relativenumber

" remove error indicators
set belloff=all

" replace tabs with spaces
set expandtab

" set tab width
set tabstop=4

" auto-ident with 4 space
set shiftwidth=4

set ruler
set wrap
syntax on
